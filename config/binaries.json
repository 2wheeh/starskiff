{
  "_comment": [
    "Binaries CI provisions for instances that are NOT container-first. An instance is",
    "container-first when it can source a node from an image — either the chain's own",
    "official image pinned to the live-network version (e.g. xplad), or one starskiff",
    "builds and publishes itself (config/images.json, e.g. evmd). The entries below source",
    "from an upstream prebuilt binary instead (no usable image, or not a chain node), per",
    "the reason on each. ci.yml downloads and verifies them directly from upstream releases.",
    "Re-check each `reason` on bump — if the image situation changed, move the chain to a",
    "container-first instance (official image) or to config/images.json (we publish it)."
  ],
  "binaries": [
    {
      "name": "cronosd",
      "repo": "crypto-org-chain/cronos",
      "ref": "v1.7.8",
      "reason": "no usable official public node image; the instance requires an injected image or binary",
      "buildType": "prebuilt",
      "asset": "cronos_1.7.8_Linux_x86_64.tar.gz",
      "sha256": "3840147724054449e3b8dd9a3829b88979da628686720dc16a9f39a5fbc911d4",
      "extract": "tar",
      "binaryPath": "bin/cronosd"
    },
    {
      "name": "dydxprotocold",
      "repo": "dydxprotocol/v4-chain",
      "ref": "protocol/v9.6.4",
      "reason": "no usable official public node image; the instance requires an injected image or binary",
      "buildType": "prebuilt",
      "asset": "dydxprotocold-v9.6.4-linux-amd64.tar.gz",
      "sha256": "4c1d38640a424c39aadbe238e4b79ca42d80f8101e16b32c597d8c441d99d7fa",
      "extract": "tar",
      "binaryPath": "build/dydxprotocold-v9.6.4-linux-amd64"
    },
    {
      "name": "hermes",
      "repo": "informalsystems/hermes",
      "ref": "v1.13.3",
      "reason": "relayer, not a chain — the hermes instance drives the CLI and its config files directly",
      "buildType": "prebuilt",
      "asset": "hermes-v1.13.3-x86_64-unknown-linux-gnu.tar.gz",
      "sha256": "3582136824ac1984c8884b310c63940a1489f091fa45faca38ed80e5dfe7d51b",
      "extract": "tar"
    }
  ]
}
